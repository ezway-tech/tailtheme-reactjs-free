/**
 * Shared class fragments for building component variants with CVA.
 * Keeps keystrokes-per-variant low and consistent across primitives.
 */

export const focusRing =
  'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-background';

export const disabledStyles = 'disabled:pointer-events-none disabled:opacity-50';

export const transition = 'transition-colors';

export const surfaceBase = 'bg-background text-foreground';
